tool_descriptions:
  query_game_state:
    description: "The game state in one call: the active story threads with their types and weights, the clocks that have not fired, and every active or background NPC with disposition, bond, agenda, instinct, arc, description, and recent memories."
    params: {}
