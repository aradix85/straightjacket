succession:

  inheritance:
    move_name: "continue_a_legacy"
    strong_hit_fraction: 1.0
    weak_hit_fraction: 0.5
    miss_fraction: 0.0

  npc_carryover:
    active:
      keep: true
      track_fraction: 1.0
    background:
      keep: true
      track_fraction: 0.5
    lore:
      keep: true
      track_fraction: 0.5
    deceased:
      keep: false
      track_fraction: 0.0
