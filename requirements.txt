# Runtime dependencies. Authoritative source: pyproject.toml [project.dependencies].
# This file exists for run.py's venv bootstrap (pip install -r requirements.txt).
starlette>=1.0,<2
uvicorn[standard]>=0.34,<1
pyyaml>=6.0,<7
anthropic>=0.40,<1
openai>=1.50,<3
